import React from "react";
import ReactDOM from "react-dom/client";
import TallerApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TallerApp />
  </React.StrictMode>
);
